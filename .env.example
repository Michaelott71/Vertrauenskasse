DATABASE_URL="postgresql://user:password@localhost:5432/golfbox?schema=public"
JWT_SECRET="replace-with-a-long-random-secret"

# Optional: creates an ADMIN user when running `npx prisma db seed`.
ADMIN_EMAIL=""
ADMIN_PASSWORD=""
